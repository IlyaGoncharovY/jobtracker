export interface CommissionDataTypes {
    selectedStationName: string;
    selectedEmployeeName: string;
    formattedDate: string;
    commissionRemarks: string;
}

export interface VerificationDataTypes {
    selectedRadioStationName: string;
    formattedDate: string;
    radioSerialNumber: string;
}
